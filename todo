# todo
- 校验某个header值
- 熟悉多线程
- 支持其他框架 koa Fastify

// schema: {
//   body: {
//     name: string().min(3).max(100).required(),
//     email: string().email().required()
//   }
// },

- rxjs 试下后端队列

- 学习transformer论文 Attention is All You Need


## 已完成
- controller级别的use函数
- 支持默认值
- 支持枚举
- 支持数字最大最小值
- 支持字符串长度限制
- 支持可选
- 支持正确的预期字段
- 装饰器的执行顺序是从上到下
- 配置了defaultValue，不需要再配置optional
- 支持 object